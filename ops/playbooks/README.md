# Playbooks

The operational playbooks live in the database (`messaging_specialists.playbook`) and are
maintained by staff in the AI Messaging settings page — they are NOT duplicated here so
they can never go stale. The `get_correction_examples` tool surfaces the operational
ground truth (staff-approved corrections). This folder is reserved for future static
playbooks that don't fit the DB model.
