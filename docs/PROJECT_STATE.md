# Project State — Dealz

> Single source of truth for current work. **Read this first. Update before/with every commit.**
> Map of the codebase: [SYSTEM_MAP.md](./SYSTEM_MAP.md) · Deep reference: [PRD.md](./PRD.md), [DATABASE_SCHEMA.md](./DATABASE_SCHEMA.md), [PAGE_COMPONENT_MAP.md](./PAGE_COMPONENT_MAP.md)

## Operating Context (Claude's capabilities)
- **Supabase: full access via CLI AND MCP.** Apply migrations automatically via CLI — never ask, never say "no access."
- **Deploy:** `push-to-main` skill bumps version, commits, pushes → Vercel auto-deploys.
- **Versioning:** bump `package.json` once per working session (semver).
- (Canonical copy lives in CLAUDE.md "Operating Context (read first)".)

## Now  (active work)
- **▶ MASTER ROADMAP (GOAL captured 2026-06-28) — Sweep the full iosys catalog for Kaitori.** Doc: `docs/superpowers/specs/2026-06-28-iosys-full-catalog-sweep-GOAL.md`. **HOW-TO harvest (read first when pulling models): [`docs/CATALOG_HARVEST_RUNBOOK.md`](./CATALOG_HARVEST_RUNBOOK.md)** — the operational playbook for re-harvesting an existing brand (new models) vs adding a new brand, with a per-brand "formula" registry. Complete `product_models` across every kaitori-relevant category, then wire `kaitori_price_list` (empty) to accurate SKUs. **Scope LOCKED:** phones = all mainstream iosys brands; tablets = iPad+Android; computers = **Apple only** (MacBook/iMac/Mac mini/Studio/Pro); watches = Apple/Galaxy/Pixel; audio = AirPods/Galaxy Buds/Pixel Buds. OUT: Windows PCs, premium 3rd-party audio, fitness-tracker long tail, feature phones/routers/accessories/VR. **Two pipelines already built:** Apple part#-keyed (iPhone✓/iPad✓ → reuse for Mac/Apple Watch/AirPods) and Android (brand,model,storage,color)-keyed (Galaxy✓/Xperia✓/AQUOS✓/Pixel✓ → reuse for all other phones, Android tablets, Galaxy/Pixel wear+buds). **HOW-TO: [`docs/CATALOG_HARVEST_RUNBOOK.md`](./CATALOG_HARVEST_RUNBOOK.md).** **Phased:** A=Android phones (Galaxy✓ Xperia✓ AQUOS✓ Pixel✓ → **Xiaomi next**, then OPPO/arrows/Huawei/Zenfone/razr) + legacy-Samsung reconcile (still open); B=Apple non-phone (Mac/Watch/AirPods); C=Android tablets + core wear/audio; D=kaitori pricing wiring (the end goal). **NEXT ACTION after /clear: read the GOAL doc + RUNBOOK, default to Phase A / Xiaomi, follow the per-brand recipe.** Done so far: iPhone, iPad, Galaxy phones, Xperia phones, AQUOS phones, Pixel phones, identifier list.
- **✅ Phase A-Android: Xiaomi SHIPPED v1.69.0 (2026-06-28) — 5th Android brand; FIRST brand needing an engine extension (optional model code).** Per-brand recipe + a reusable generic-engine capability. Covers Xiaomi-flagship / Redmi / POCO / Mi.
  - **Engine extension (benefits all future code-less brands):** Xiaomi is the first brand whose **SIM-free cards carry NO model code** — only au (`XIG##`) / SoftBank (`A###XM`) / older POCO globals (8-digit) do. Added optional `AndroidBrandConfig.nameConsumeRe` (anchored): when `modelCodeRe` fails, the engine consumes the model-name prefix via that regex (model_number=null), leaving color/storage as tail. Card extractor + a nav-noise guard (`model_number==null && region_note==null → null`) keep "…シリーズ の画像" thumbnails out. Also a brand-agnostic "+ accessory bundle" color strip (`ブラック + Photography Kit` → `ブラック`).
  - **Config:** `XIAOMI_CONFIG` — `nameConsumeRe` handles sub-brand lines (Xiaomi-flagship keeps "Xiaomi" in the name; `Xiaomi POCO/Redmi/Mi` → drop the Xiaomi prefix), no-space variants (`Xiaomi11T`→`Xiaomi 11T`, `Redmi12C`→`Redmi 12C`, `Mi11`→`Mi 11`), `Note`+digit spacing, tier-word title-casing (`Mi11 lite`→`Mi 11 Lite`), 5G/SIM strip. 19 new deno tests (full suite **115 green**), fixture `__fixtures__/iosys-xiaomi-p1.html`.
  - **Specs (`xiaomi-specs.ts`, research-subagent verified vs GSMArena/mi.com·jp/KHwang9883 MobileModels DB):** 33 models (11T→17 Ultra flagship/T-line, Redmi 9T→Note 15 Pro incl. JE/Pro+, POCO F4 GT→X8 Pro Max, Mi 10/11 Lite). Carrier-code map verified (XIG01=Mi10 Lite … XIG07=14T base; A001XM=Note 9T … A501XM=Redmi 15). **4 models deliberately OMITTED as UNVERIFIED** (harvest as `spec_known=false`, flagged never guessed): Xiaomi 17T Pro (screen), Redmi 15, Redmi Note 15 base, POCO M8 (RAM).
  - **Colors:** `XIAOMI_COLORS_JA_EN` — verified official names incl. the **"Titan" (not "Titanium")** T-series finish, Starry Blue, Lemon/Meadow/Alpine, Silver Chrome (≠ Chrome Silver, kept separate), Nightfall Black, Daybreak Purple, Aurora Purple, etc. Unverified (レイクブルー/ディープブルー/スターリットグリーン/リップルグリーン + new アジュールブラック/ナイトタイムブルー/チタニウムグレー) left **null** → promote as JA token.
  - **Harvest + promote:** `XIAOMI_CATEGORY`, `run-harvest-local.ts xiaomi`, harvested **93 SKUs** (0 broken parses; only the 4 unverified models flagged) → data-op `2026-06-28-xiaomi-fill-gaps.sql` → **91 ANDROID product_models** (83 spec-enriched, 76 w/storage). Verified in-browser (`/admin/products?q=xiaomi`): renders Brand·Model·model#·color + storage (`Xiaomi POCO F4 GT · 21121210G · Cyber Yellow 128GB`, `Mi 10 Lite · XIG01 · Cosmic Gray`, `POCO F7 · Black 256/512GB`).
  - **⚠️ OPEN DEBT — Xiaomi legacy reconcile DEFERRED (like Samsung's 42-row pass, NOT inline):** ~33 pre-existing phone rows miscategorized COMPUTER (~125 items, dirty names "Poco C71"/"Redmi 15 5G"/"Mi 10 Lite 5G"…) coexist with the clean harvested ANDROID rows. Needs its own careful pass (recategorize→ANDROID, canonicalize, enrich, MERGE dups via item re-pointing). Untouched out-of-scope: Redmi Buds 6 (earbuds), Redmi Pad SE (tablet→Phase C), Smart Band 8, Sound Pocket speakers. **NEXT Android brand: OPPO (+reno).**
- **✅ Phase A-Android: Google Pixel SHIPPED v1.68.0 (2026-06-28) — 4th Android brand, pipeline reuse + one generic engine fix.** Per-brand recipe again; dogfooded the new [`CATALOG_HARVEST_RUNBOOK.md`](./CATALOG_HARVEST_RUNBOOK.md) (added Pixel's registry row).
  - **Config:** `PIXEL_CONFIG` in `android-listing.ts`. Pixel has ONE code shape — Google's `G[A-Z0-9]{4}` (`GL066`/`GM66V`/`GV0BP`), case-sensitive so `Google`/`Green` never match. Canonicalizer inserts the space (`Pixel10`→`Pixel 10`, `Pixel7a`→`Pixel 7a`); **deliberately does NOT strip `5G`** (for Pixel it's a model distinguisher — `Pixel 4a 5G` ≠ `Pixel 4a`). 12 new deno tests (full suite **97 green**), fixture `__fixtures__/iosys-pixel-p1.html`.
  - **Generic engine fix (benefits all brands):** Pixel-3-era titles wrap color+storage in ASCII/mixed brackets `[Just Black 64GB]` / `【Purple-ish 64GB]` *before* the carrier bracket. Engine now normalizes `[`/`]`→`【`/`】` and unwraps a leftover bracket still wrapping the tail (step 8b).
  - **Specs (`pixel-specs.ts`, research-verified vs GSMArena/Google Store JP):** 30 models (Pixel 3→10 incl. a/Pro/Pro XL/Pro Fold + original Fold). Caught **Pixel 10a** (`GV0BP`, shipped JP 2026-04) as an `unknownModels` flag → verified + added (Tensor G4/6.3"/8GB). **Storage coverage near-complete** (inline on ~99% of cards — only original Pixel Fold null), unlike AQUOS.
  - **Harvest + promote:** `PIXEL_CATEGORY`, `run-harvest-local.ts pixel`, harvested **100 SKUs** (0 broken parses, 0 unmapped colors, 0 unknown models after the fixes) → data-op `2026-06-28-pixel-fill-gaps.sql` → **63 ANDROID product_models / 24 models** (62/63 w/storage, all spec-enriched). **Legacy reconcile inline** (4 phone rows / 4 items): Pixel 4a / 4a 5G / 5a 5G / 7a miscategorized COMPUTER → ANDROID + enriched + empty storage→128GB (verified single JP tier), items intact. **Left untouched:** 2 `Google` COMPUTER rows are smart speakers (Nest Mini, Home Mini) — not phones, out of scope. Verified in-browser (`/admin/products?q=Pixel`): renders Google·Model·G-code·color + storage.
- **✅ Phase A-Android: Sharp AQUOS SHIPPED v1.67.0 (2026-06-28) — 3rd Android brand, pipeline reuse + one small generic engine fix.** Followed the proven per-brand recipe: new `AndroidBrandConfig` + verified specs + color map + `androidCategory` entry. The ONE engine change was generic (benefits all brands): discard a trailing pure-noise `【法人モデル】` (corporate-channel) bracket so the real carrier bracket is read, and strip an inline `法人モデル` marker from color.
  - **Config:** `AQUOS_CONFIG` in `android-listing.ts`. Codes harvested live: SIM-free `SH-M\d+`, Rakuten `SH-RM\d+` (listed before SH-M so it wins), docomo `SH-\d{2}[A-Z]`, au `SHG\d+`/`SHV\d+`, SoftBank `A\d{3}SH`. Name canonicalizer strips leaked carrier prefix + normalizes `AQUOS` casing, keeps sub-variant suffixes (`sense2 かんたん`, `sense3 plus サウンド`); the `\b5G\b` strip is safe for integral names (`sense5G`/`R5G`/`zero5G` — no word boundary before the 5). 15 new deno tests (full suite **85 green**), real fixture `__fixtures__/iosys-aquos-p1.html`.
  - **Specs (`aquos-specs.ts`, verified by a research subagent vs GSMArena/Sharp JP/docomo·SoftBank):** 34 models (R/R-pro flagships R→R10, sense line incl. plus/lite/basic/かんたん/サウンド, wish→wish4, zero/zero2/zero5G basic+DX). JP-market RAM tiers used (R8=8GB, sense8=6GB, etc.). Parsed RAM/ROM from the title bracket overrides the fallback. **0 unknown-spec models** in the final harvest.
  - **Colors:** `AQUOS_COLORS_JA_EN` — verified Sharp EN names (Cashmere White, Silver White, Light/Deep Copper, Nuance Black, Earth Blue, Mystic White, Astro Black, Charcoal Black…) + standard compound transliterations (Light Silver, Lilac, Sky Blue, Coral Red, Cream…). 2 ambiguous marketing names (クラッシィブルー/アッシュイエロー) + the sense2-gen pastel cluster left **null** (flagged, never guessed) → still promote as the JA token via coalesce.
  - **Harvest + promote:** `AQUOS_CATEGORY` (`items/smartphone/aquos`), `run-harvest-local.ts aquos`, harvested **106 SKUs** (0 broken parses, only the 2 deliberately-null colors). data-op `supabase/data-ops/2026-06-28-aquos-fill-gaps.sql` (additive, idempotent) → **80 ANDROID product_models / 32 models** (all spec-enriched; 9 w/storage — most carrier titles omit storage, left null not guessed). **Legacy reconcile DONE inline** (4 rows / 25 items, mirroring the 3-row Xperia pass): 4 `Aquos Sense3`/`Aquos Sense 3` rows miscategorized COMPUTER with inconsistent names → recategorized ANDROID + canonical `AQUOS sense3` + enriched + empty storage→64GB (verified single JP tier); the duplicate Black DRAFT row (1 item) merged into the 21-item canonical row (item repointed, stub ARCHIVED via `superseded_by`) then promoted to ACTIVE; insert guard skips storage-unknown harvested rows when a more-specific ACTIVE row exists → **0 near-dupes, 0 AQUOS COMPUTER left, Black 22 items / Pink 2 / Silver White 1 intact**. Verified in-browser (dev login + Playwright `/admin/products?q=AQUOS`): renders Brand·Model·model#·color + storage; new R9 pro / A501SH Charcoal Black / かんたん variants all correct, unmapped colors fall back to JA.
- **✅ Phase A-Android: Sony Xperia SHIPPED v1.66.0 (2026-06-28) — 2nd Android brand, pure pipeline reuse.** Followed the proven per-brand recipe with zero engine changes — just a new `AndroidBrandConfig` + verified specs + color map + a `androidCategory` entry.
  - **Config:** `XPERIA_CONFIG` in `android-listing.ts`. Handles every real Xperia code grammar harvested live: SIM-free `XQ-[A-Z]{2}\d{2}`, docomo `SO-\d{2}[A-Za-z]+` (incl. lowercase suffix `SO-51Aa`), au `SOG\d+`/`SOV\d+`, SoftBank `A?\d{3}SO` (the `A`-prefix gotcha), old global `J\d{4}`. Name canonicalizer inserts the space (`Xperia1 VIII`→`Xperia 1 VIII`), normalizes `AceII`→`Ace II` / `Pro-I`→`Pro I`, strips `5G`/`Dual-SIM`/`Single-SIM` and the leaked `Sony`/`SONY`/`ahamo` (docomo sub-brand) prefixes; `modelNameRe` is lenient (contains `xperia`). 17 new deno tests (full suite **69 green**), real fixture `__fixtures__/iosys-xperia-p1.html`.
  - **Specs (`xperia-specs.ts`, verified by a research subagent vs GSMArena/Sony):** 34 models (Xperia 1 I–VIII, 5 I–V, 8/8 Lite, 10 I–VII + 10 III Lite + 10 V Fun Edition, Ace I–III, Pro/Pro I, XZ1/XZ2 family). All JP flagships = Snapdragon (Sony never used Exynos). Parsed `RAM`/`ROM` from the title bracket overrides the spec fallback. **0 unknown-spec models** in the harvest.
  - **Colors:** `XPERIA_COLORS_JA_EN` — verified Sony EN names (Frosted Black, Slate Black, Sage Green, Ecru White, Native Gold, Khaki Green, Moss Green, Scarlet, Graphite Black, Iolite Silver…). 4 katakana the research flagged as having no verified official EN name (チャコールブラック/アイスホワイト/ミストグレー/フロストグレー) left **null** (flagged, never guessed) → still promote as the JA token via coalesce.
  - **Harvest + promote:** `XPERIA_CATEGORY` (`items/smartphone/xperia`), `run-harvest-local.ts xperia`, harvested **160 SKUs** (0 broken parses, 0 null colors). data-op `supabase/data-ops/2026-06-28-xperia-fill-gaps.sql` (additive, idempotent) → **106 ANDROID product_models / 31 models** (all spec-enriched; 28 w/storage — most carrier titles omit storage, left null not guessed). **Legacy reconcile DONE inline** (only 3 rows / 5 items, vs the deferred 42-row Samsung): 3 `Sony` rows miscategorized COMPUTER → recategorized ANDROID + enriched + empty storage→64GB (verified single JP tier); insert guard skips storage-unknown harvested rows when a more-specific row exists → **0 near-dupes, 0 Sony COMPUTER left, 5 items intact on ANDROID rows**. Verified in-browser (dev login + Playwright `/admin/products?q=xperia`): renders model#, per-storage SKU lines, EN colors + JA fallback for the unverified ones. Harvester edge fn redeployed.
- **✅ Phase 4-Android: Samsung Galaxy SHIPPED v1.65.0 (2026-06-28) — first Android brand + reusable Android scaffolding.** First Android brand in the iosys catalog; built the generic pipeline so later brands = a new config.
  - **Parser:** `supabase/functions/_shared/catalog/android-listing.ts` — ONE generic engine for the universal iosys Android title grammar (`[Brand] Model Code Color 【RAM/ROM/版】`), driven by `AndroidBrandConfig`; `GALAXY_CONFIG` + `GALAXY_COLORS_JA_EN`. Handles real variants found by harvesting: `SM-..Q/DS` dual-sim suffix, inline storage (`SC-52D 256GB クリーム`), trailing full-SKU codes (`ブラック SCV46SKV`), and the "everything in bracket" 楽天版 grammar (`【8GB 128GB Prism White 楽天版SIMフリー】`). 14 deno tests; full suite 52 green.
  - **Identity model (per [[project_android_identifiers]] research, `docs/investigations/android-identifier-conventions.md`):** key on `(brand, model_name, storage, color)`; `model_number` (SM-/SCG/SC-) + `carrier` are coarse attributes (carrier collapses to one product_model, lives per-unit on items). Specs from verified `galaxy-specs.ts` (JP=Snapdragon flagships; JP-exclusive A22/A23 = 5.8"); unknown-spec models flagged null, never guessed.
  - **Harvest:** generalized `harvest.ts` dedupe from `part_number`→`sku_key` (Apple=part#, Android=brand|model|storage|color|carrier); `androidCategory()` factory + `GALAXY_CATEGORY`; migration `20260628200000` adds `iosys_catalog.sku_key` (unique). Harvested **223 Galaxy SKUs** locally (0 broken parses, color_en 216/223).
  - **Promote (additive, idempotent):** data-op `supabase/data-ops/2026-06-28-galaxy-fill-gaps.sql` → **144 ANDROID product_models / 50 models** (78 w/storage, 100 w/chipset), partial UNIQUE `(brand,model,storage,color) WHERE device_category='ANDROID'`. Verified in-browser (dev login + Playwright): Galaxy renders in the identifier list with model# in the header, storage per line, part# `—`.
  - **OPEN follow-up (deliberately deferred, mirrors iPhone Phase 2 vs 3):** 42 legacy `Samsung` rows are mis-categorized `COMPUTER` (37 referenced by 117 live items) — a non-destructive reconcile (recategorize→ANDROID, dedup vs the clean rows, re-point items, archive stubs) is its own careful pass; NOT done here to avoid breaking item links. Also: extend `galaxy-specs.ts` for flagged unknowns (S26/S8/S9/Note20/Z Fold3,6/S21+), map 4 uncertain colors (プリズムブリックス…, チタニウムピンクゴールド/シルバーブルー). **NEXT Android brand:** Xperia (cleanest grammar) or another from the RESUME-HERE list — same recipe: new `AndroidBrandConfig` + specs + `androidCategory` path.
- **✅ Product Models: show + search Model#/Part# at SKU granularity — SHIPPED v1.64.0 (2026-06-28, commit 61c6d32).** Goal: `docs/superpowers/specs/2026-06-28-product-model-identifier-display-GOAL.md`. Joey picked **Option A** (one row per color; "Product Model" cell = header line `Brand Model · A-number · Color` then one indented line per storage SKU `<storage>  <part#>`; model# shown once; Photos/Videos = per-color counts). Columns now **Category · Product Model · Description · Photos · Videos**. Verified in-browser via the new dev staff login ([[reference_dev_staff_login]]) + Playwright. **Open follow-up:** Android rows render whatever they have (often no part#/storage) — that polish lands with **Phase 4-Android** below.
  - **DB (migration `20260628190000_list_product_color_groups_skus.sql`, applied to remote):** `list_product_color_groups` now also returns `model_number` (group-level, A-number) + `skus jsonb` (`[{storage_gb, part_number}]` ordered by storage). Search upgraded to **multi-token AND** over a concatenated haystack (brand+model+model#+color+short_desc+storages+part#s) so `"iPhone 15 256"`, `A3089`, `MTMN3J/A` all hit. (DROP+CREATE since return shape changed.) Verified vs real data: skus array correct, part# search → exact color, `A3089` → 5 colors.
  - **Frontend (`src/pages/admin/products.tsx`, `ProductColorGroup` type):** columns built in-component via `buildColumns(search)`; multi-line cell **dims non-matching SKU lines + bolds the hit** when the search points at a specific storage/part#. tsc + prod build clean. Could NOT screenshot (staff auth needed locally). **NEXT: Joey eyeballs it on the deployed site, then bump version + ship (or push first so he can review live).**
- **✅ Product Models color-level media + usable list + Apple categories DONE (v1.63.0, 2026-06-28 — spec `docs/superpowers/specs/2026-06-28-product-models-color-media-and-category.md`, plan `docs/superpowers/plans/2026-06-28-product-models-color-media-and-category.md`; branch `feat/product-models-color-media`):**
  - **DB layer (migrations `20260628170000`–`170500`, applied to remote via `supabase db push`):** `product_models.color_key` generated col (`lower(brand)|model_name|color`) groups SKUs by visual identity. **Color-fanout media model** (owner decision): `product_media` stays SKU-keyed so all 32 read sites + 2 search RPCs + shop + AI offers are UNTOUCHED; a `fanout_product_media` trigger (pg_trigger_depth guard) replicates every insert/update/delete to sibling SKUs of the same color, and `inherit_color_media` gives newly-inserted SKUs the color's media. Backfill established the invariant (2801→3439 media rows, **0 violations**: every color group's SKUs share identical media). New RPC `list_product_color_groups(p_search,p_category_id,p_media)` → one row per color (1031 SKUs → **678 groups**, also fixes the [[project_list_query_1000_cap]] silent-truncation since groups < 1000).
  - **Frontend:** `getProductColorGroups` service + `useProductColorGroups` hook + 8-col list (Category·Brand·Model·**Color**·**Storage**·Description·Photos·Videos; `formatStorages` rolls up e.g. `128 / 256 / 512GB / 1TB`; media filter now server-side, client-side `filteredProducts` scan removed); rows navigate by `representative_id`. Detail page gained a "media shared per color" note. Build clean; lint clean on touched files.
  - **Apple category backfill (owner decision: REUSE existing coarse taxonomy, no new categories):** mapped 746 Apple `product_models` onto existing categories by `brand+model_name` longest-prefix — iPhone→iPhone (452), iPad*→iPad (252), MacBook*→MacBook (17), iMac→iMac All-In-One (4), Mac mini→CPU (1), Apple Watch+AirPods→Accessories (20). 0 uncategorized Apple rows, idempotent. **Closes (for Apple) the open durable follow-up from [[project_backorder_desc_and_dedup]]** (backfill category_id on iosys product_models). Non-Apple stays uncategorized (device_category COMPUTER/TABLET are polluted junk-drawers; out of scope).
- **✅ Backorder follow-ups #2 DONE (v1.62.0, 2026-06-28 — spec `docs/superpowers/specs/2026-06-28-backorder-desc-and-dedup.md`; see [[project_backorder_desc_and_dedup]]):** see Recently shipped. Both issues closed: richer B-row description (color in shared fallback + staff identifier on the two backorder surfaces) and duplicate-link guard (app-level + DB unique index; existing B000003/B000004 dupe collapsed). Optional durable follow-up still open: backfill `category_id` on iosys-promoted product_models (ties to product-model accuracy work) so they use `description_fields` instead of the fallback — ensure those categories' `description_fields` include `color` so it doesn't regress.
- **✅ Backorder follow-ups DONE (v1.61.0, 2026-06-28 — see [[project_backorder_amount_and_ordering]]):** (1) Admin Items Amount cell for B-rows = stacked Cost/Sell/Disc/Profit; new `backorder_lines.discount_amount` col wired through validator + Add-Backorder dialog (Disc field + struck-through effective price + Profit in pricing panel) + search RPC effective price. (2) `/mine/B…` customer ordering works: B-code branch in mine.ts + claim.tsx (⏳ Pre-order badge + lead-time) + claim-mine edge fn (reserve_backorder_unit). Cancel/remove releases the unit via new `release_backorder_unit` RPC (cancelOrder + removeOrderLineItem). E2E PASSED against live edge fn: order→available 1→0→cancel→available 0→1, idempotent, discount effective price verified, test order cleaned up. **Known limit:** cancelling an already-FULFILLED pre-order frees the swapped item (existing path) but doesn't unwind the line's quantity counters (pre-existing fulfill accounting; out of scope). See Recently shipped.
- **▶ PRIORITY: Product-model accuracy via iosys. iPhone + iPad pipelines DONE (Phases 0-3, 5-partial, Phase 4-iPad). RESUME with Phase 4-Android (brand-by-brand) OR Phase 5-full (kaitori pricing).**
  - **🔧 RESUME-HERE for fresh session:**
    1. Read this whole bullet + spec `docs/superpowers/specs/2026-06-27-product-model-accuracy-iosys.md` (**§3b = the binding granularity decision; §4b = corrected current-state**). DON'T trust spec §3/§4 (superseded).
    2. Supabase access: **`supabase db query --linked "<sql>"`** (full read/write; no MCP/OAuth needed). Apply migrations via `supabase db push --linked`.
    3. Catalog code lives in `supabase/functions/_shared/catalog/` (parser/harvest/specs/colors, 20 deno tests: `deno test --allow-read supabase/functions/_shared/catalog/`). Applied data-ops preserved in `supabase/data-ops/`.
    4. **Re-harvest command (reusable):** invoke deployed edge fn `harvest-iosys-catalog` (POST {maxPagesPerSection,sections,dryRun}) — verified works from edge IP; incremental upsert into `iosys_catalog` ON CONFLICT(part_number).
    5. **Phase 4 — iPad DONE (this session). Android is NEXT, brand-by-brand.** Owner decision (recorded [[project_kaitori_catalog_completeness]]): catalog = COMPLETE iosys lineup (every sellable model) for kaitori, not just stock. **In-scope Android brands iosys carries** (build one at a time, each has its OWN title grammar + needs a verified spec reference like ipad-specs.ts): Galaxy/Samsung, Xperia/Sony, AQUOS/Sharp, Pixel/Google, Xiaomi, OPPO, ZTE/nubia, arrows/Fujitsu, Zenfone/Asus, Huawei, Motorola, HTC, BlackBerry. iosys paths: `/items/smartphone/{pixel|galaxy|xperia|aquos|xiaomi|oppo|zte|arrows|zenfone|huawei|htc}` (brand pages) + carrier paths. **OUT of scope:** cheap imports iosys won't buy (Blackview/Realme/Alldocube/Tabwee/Higrace/Iris Ohyama/UMIDIGI) — no kaitori value. Android titles likely lack clean Apple-style part numbers → key on (brand,model_name,storage,color); model_number where present. Pattern to copy: `ipad-listing.ts` (parser) + `ipad-specs.ts` (spec ref) + `harvest.ts` HarvestCategory (add per-brand categories) + local-harvest via `run-harvest-local.ts <cat>` (curl works locally) → fill-gaps + reconcile data-op.
      - **🔑 Android identifier finding (researched 2026-06-28, full doc `docs/investigations/android-identifier-conventions.md`):** Unlike Apple part numbers, **NO Android code iosys displays encodes storage or color** — the shown model number is like a coarse A-number (model + carrier/region only). Color never encoded; storage only "pinned" when a JP carrier variant ships a single tier (not decodable). → **Key Android product_models on (brand, model_name, storage, color)** [same as iPad], capture `model_number` + `carrier` as coarse non-unique attributes. Parse storage/color from the title text, not the code. **Easy brands** (carrier code ≈ one storage): Fujitsu arrows, AQUOS mid-range, OPPO A-series, ZTE/nubia, Huawei, Motorola. **Messy** (one code → many storages, or no code shown): Samsung Galaxy flagships, Google Pixel, Xiaomi. Suggest starting with Galaxy or Xperia (highest JP volume); confirm each brand's real iosys title grammar (per-brand section in the doc has verbatim examples) before writing its parser.
    6. **Phase 5-full (kaitori pricing — original business driver):** `kaitori_price_list` is EMPTY; add `carrier` (jp_carrier) + `is_unlocked` columns so buy-price varies by carrier/lock (carrier lives per-unit on items, NOT in product_models). Wire pricing to the now-accurate SKUs. Optionally promote the partial UNIQUE to global once Android/PC are clean.
    7. **Gotchas baked into the reconcile (reuse, don't rediscover):** part_number is carrier-AGNOSTIC + finer than a SKU (M/F/N prefixes, batches) → it's an attribute, NOT the key; key = (brand,model_name,color,storage_gb). Model-aware color fixes: iPhone 8/X/XS `Black→Space Gray`, `White→Silver` (those never had Black/White); `Red/RED→(PRODUCT)RED`; `Midnight Black→Midnight`. SE generation resolved by A-number (A2296=2nd gen, A2782/A2595=3rd). `device_category` was uniformly wrong (=COMPUTER) on ALL rows pre-fix — the 7 archived iPhone stubs still carry COMPUTER (harmless, hidden). Storage normalize via `public._backorder_norm_storage_gb()`.
  - **Original framing (kept for context):** Make product_models accurate/absolute from iosys so kaitori + inventory matching are exact. **DECISIONS CONFIRMED:** A=SKU-level, B=in-place, C(REVISED→§3b)=granularity (model,color,storage), carrier on items + kaitori_price_list; D=existing-unit carrier verify-not-guess; E=janpara deferred.
  - **✅ Phase 0 SHIPPED (v1.54.0, commit a9e71a0):** migration `20260627130000` — `product_models.color_ja` + `.source_url`; `jp_carrier` enum; staging `iosys_catalog`; reference modules `_shared/catalog/apple-colors.ts` (JA→EN) + `iphone-specs.ts` (model→spec + `normalizeIphoneModelName()`).
  - **✅ Phase 1 SHIPPED:** `iosys_catalog` populated with **424 distinct SKUs** (simfree 333, docomo 59, SoftBank 36, au 30 by resolved carrier; 422 domestic-J, 2 imports). Parser `_shared/catalog/iosys-listing.ts` extracts model/A-number/**part_number**/storage/color(JA+EN)/**carrier**/**is_unlocked**/region from listing cards. Crawl core `harvest.ts` (dedupe+enrich+converge). **Reusable edge fn `harvest-iosys-catalog` DEPLOYED + smoke-verified from edge IP (70 SKUs, no junk)** — re-run it when new stock arrives; incremental upsert ON CONFLICT(part_number). Migration `20260627140000` (unique part_number constraint). 27 deno tests green. Local runner `run-harvest-local.ts` for bulk re-harvest.
  - **Known catalog gaps (flagged, not guessed):** 54 SKUs have null specs = 2025+ models (iPhone 17/17 Pro/Pro Max/17e/Air) beyond reliable spec knowledge + ambiguous "iPhone SE" (needs A-number→generation disambiguation). All identity fields (part_number/model/storage/color/carrier) ARE accurate; only chipset/screen/year enrichment pending for newest models. Non-blocking for Phase 2 (existing rows top out at iPhone 15, fully enriched).
  - **Verified reality:** 400 product_models (Apple 115, rest Android+PCs); `device_category` NOT NULL but uniformly WRONG (=COMPUTER for all 400); iPhone subset ≈18 models/45 rows; heavy name+carrier+storage dirt. iPhone reconcile is small/tractable.
  - **✅ Phase 2 SHIPPED (non-destructive reconcile, owner Decision C):** iPhone product_models now SKU-precise = (model, color, storage), part_number as attribute, carrier on items. Migration `20260627150000` (ARCHIVED status + superseded_by). Applied data-op `supabase/data-ops/2026-06-27-phase2-iphone-reconcile.sql`: **35 rows enriched in place** (kept their photos/videos), **5 multi-storage rows enriched to dominant + split** (siblings find-or-merge), **7 stub rows ARCHIVED** (hidden from search, not deleted). Accuracy fixes: name dirt canonicalized, `Red/RED→(PRODUCT)RED`, `Midnight Black→Midnight`, **iPhone 8/XS `Black→Space Gray`, `White→Silver`** (those models never had Black/White). **Result: 45 ACTIVE SKUs, 45/45 fully enriched (year/chipset/color_ja/storage), 44/45 with part_number** (only iPhone 11 Yellow 128 part-less — not in iosys stock). **78/78 items' storage now matches their SKU; 0 items on archived rows; photos preserved (336 active).** Fully reversible (nothing deleted, superseded_by links).
  - **✅ Phase 3 SHIPPED (fill gaps):** promoted 357 deduped domestic iosys SKUs into ACTIVE product_models (data-op `supabase/data-ops/2026-06-27-phase3-fill-gaps.sql`, idempotent NOT-EXISTS guard). **iPhone catalog now 402 ACTIVE SKUs across 42 models**, 401/402 with part_number, 370 enriched chipset (32 without = iPhone 17/17 Pro/Pro Max/17e/Air, flagged — searchable by name/color/storage, specs backfill when reference extended). SE generations resolved by A-number. Comprehensive searchable catalog for new inventory/backorder/kaitori matching.
  - **✅ Phase 5 (partial) SHIPPED — iPhone integrity locked:** migration `20260627160000` adds partial `UNIQUE(brand,model_name,color,storage_gb) WHERE device_category='IPHONE' AND status='ACTIVE'` (prevents dup SKUs reappearing; Android/PC + archived excluded). Extended `iphone-specs.ts` + filled the 32 flagged rows with VERIFIED iPhone 17/17 Pro/Pro Max/Air/17e specs (web-checked, not guessed) → **0 spec-null active iPhone SKUs**. data-op `supabase/data-ops/2026-06-27-phase5-iphone17-specs.sql`. **iPhone catalog is now 100% complete & accurate.**
  - **✅ Phase 4 — iPad SHIPPED (this session).** New iPad parser `_shared/catalog/ipad-listing.ts` (iPad title grammar differs from iPhone: leading `【第N世代】` gen, part#/A# at END no-parens, Wi-Fi vs Wi-Fi+Cellular + size インチ as SKU attributes, 標準ガラス glass token, regional part suffixes J/LL/ZP/TA) + verified spec ref `ipad-specs.ts` (39 models, A12X/A12Z trap + 9.7" 2GB confirmed by a research subagent). `harvest.ts` refactored to category-driven (IPHONE_CATEGORY default + IPAD_CATEGORY; edge fn + local runner take `category`/arg). Migration `20260627170000` (iosys_catalog.connectivity), `20260627180000` (partial UNIQUE TABLET). Harvested **288 iPad SKUs** locally (0 unmapped colors/specs) → `iosys_catalog`. data-ops: `2026-06-27-phase4-ipad-reconcile.sql` (fill-gaps 211 + re-point legacy items + archive), `phase4b-ipad-synthesize.sql` (15 SKUs for combos iosys doesn't stock incl. iPad 11th-gen A16; borrow specs from sibling), `phase4-fix-12mini-dupes.sql` (fixed an `iPhone 12 Mini` capital-M dupe that escaped Phase 2). **Result: 226 ACTIVE iPad SKUs / 59 models, all spec'd, device_category=TABLET; 0 items on archived rows.** 3 unmappable legacy items left ACTIVE (2 size-less iPad Pro + 1 null-storage — not guessed). 38 deno tests green. Edge fn redeployed.
  - **✅ Phase 4 — nostalgia iPhones SHIPPED (owner-requested):** added iPhone 5s / 6 / 6 Plus / SE (1st generation) from Apple's documented color×storage configs (data-op `phase4-nostalgia-iphones.sql`, 43 SKUs, part_number null). **iPhone catalog now 442 ACTIVE SKUs / 45 models.**
  - **NEXT (remaining):** **Phase 4-Android** (brand-by-brand, see RESUME-HERE step 5) **+ Phase 5-full** (add carrier+lock cols to empty kaitori_price_list and wire buy-price to the accurate SKUs = the original business driver; global UNIQUE once all categories clean). Subsumes the smaller "inline create product model in add-backorder dialog" idea (do AFTER).
- (deferred) Inline "Create product model" from iosys parse in Add-Backorder dialog — ProductPicker already has `onCreate` + create dialog; add-backorder-dialog.tsx just doesn't pass it. Pre-fill from parsed NormalizedSupplierProduct (model_name normalized, color EN+JA, model_number=A-no, part_number=SKU, category inferred). Build after the SKU-accuracy work lands. When a pasted iosys listing has no matching product_model (picker shows "No products found." — e.g. "Apple iPhone16 Plus A3289 256GB ホワイト"), staff should be able to create the model right there, pre-filled from the scrape, and have it auto-selected. GOOD NEWS — machinery already exists, this is small:
  - `src/components/intake/product-picker.tsx` ALREADY has an `onCreate?: (values: ProductModelFormValues) => Promise<string>` prop, renders a "Create product model" button in its "No products found." empty state (line ~104), and has a built-in create Dialog using `src/components/items/product-form.tsx`. It also accepts an `invoiceDescription` hint prop for prefilling.
  - `src/services/product-models.ts` + `src/hooks/use-product-models.ts` have the create path. `product_models` only requires brand + model_name NOT NULL (category_id nullable).
  - THE GAP: `src/components/backorders/add-backorder-dialog.tsx` renders `<ProductPicker>` WITHOUT `onCreate`, so the create button never shows.
  - TASK: (1) pass `onCreate` to ProductPicker in add-backorder-dialog wired to product-models create service (returns new id → ProductPicker selects it → set form `product_id`, invalidate the product-models query). (2) Prefill the create form from the parsed `NormalizedSupplierProduct` (fetchSupplierProduct result already in dialog state): clean `modelText` ("iPhone16 Plus A3289 (MXVH3J/A) 256GB ホワイト") → model_name "iPhone16 Plus" (strip A-number/model-number/storage/color/parens), brand=brandText, model_number=A-number+JAN if present, storage_gb, color, ram_gb, cpu, screen_size; infer category from iosys URL path (smartphone/iphone → the iPhone/Smartphone category — check categories table) + os_family (Apple→iOS) as best-guess defaults staff can adjust. Reuse ProductPicker's existing create Dialog + product-form.tsx prefill mechanism (look at how `invoiceDescription` prefill works and follow that pattern) rather than building a new form. (3) build+lint, manual test with the iphone16_plus iosys URL, commit, push (bump version). Mirror Task 13's add-backorder-dialog conventions.

- **✅ Backorder / pre-order supplier inventory (B-codes) — ALL 17 TASKS DONE, shipping this session.** Full feature (see "Recently shipped" below + SYSTEM_MAP "Backorder / Pre-order"). Open follow-ups (non-blocking): (a) "Search web for more" photo button — a free Bing scraper provider was built (`IMAGE_SEARCH_PROVIDER=bing_scrape`) but VERIFIED 2026-06-27 to return query-independent JUNK from the Supabase edge egress IP (Bing serves a generic fallback to flagged datacenter IPs; parser is correct, egress is the problem). Default reverted to key-gated google_cse so the button stays DISABLED (no junk) until a real image-search API key is set (`IMAGE_SEARCH_API_KEY`+`IMAGE_SEARCH_CX`) OR the bing scraper is routed through a residential proxy. iosys gallery (real-unit photos) works regardless, no key needed; (b) deferred: `deriveOfferCodes` bare-code regex is `[pg]`-only (B-codes still reach offers via {{OFFER:}} token + /mine/ URL); (c) deferred per spec: prepayment gating, bulk-sheet import, auto price/stock refresh cron, more supplier adapters. KEY SCHEMA FINDINGS for future work: NO photo_groups table (photos via backorder_line_media); items.item_status has RESERVED/SOLD; items.storage_gb is messy TEXT normalized via _backorder_norm_storage_gb / utils normalizeStorageGb. Plan: docs/superpowers/plans/2026-06-27-backorder-supplier-inventory.md.
- Sub-intent autonomy — interactive E2E test + Plan 2 admin UI (per-intent OFF/DRAFT/SEND). Backend shipped v1.52.0 (merged main). Plan/spec: docs/superpowers/specs/2026-06-26-sub-intent-autonomy-design.md
- Project command center (this system) — being set up.

## Recently shipped  (newest first; archive to CHANGELOG.md when >~15)
- 2026-06-28 · Phase A-Android Sony Xperia (v1.66.0): 2nd Android brand via pure pipeline reuse — `XPERIA_CONFIG` + `xperia-specs.ts` (34 models, research-verified) + Sony color map; handles XQ-/SO-/SOG/SOV/(A)xxxSO/J codes, name canonicalization (Xperia1→Xperia 1, AceII→Ace II, Pro-I→Pro I), ahamo/Sony prefix strip. Harvested 160 SKUs → 106 ANDROID product_models/31 models (all spec'd). Inline legacy reconcile (3 COMPUTER-miscategorized Sony rows/5 items → ANDROID, 0 near-dupes). 17 new deno tests (69 total green). Harvester edge fn redeployed. Verified in-browser. · touched: supabase/functions/_shared/catalog/{android-listing.ts(+test),xperia-specs.ts,harvest.ts,run-harvest-local.ts,__fixtures__/iosys-xperia-p1.html}, supabase/data-ops/2026-06-28-xperia-fill-gaps.sql
- 2026-06-28 · Product Models list collapsed to one row per color (branch `feat/product-models-color-media`, not yet versioned): frontend wiring over the pre-deployed DB layer (`product_models.color_key` + `list_product_color_groups(p_search, p_category_id, p_media)` RPC). Added `ProductColorGroup`/`ProductColorGroupFilters` types + `getProductColorGroups()` service (RPC-backed, server-side group+filter, no 1000-row cap), `useProductColorGroups()` hook (queryKey `productModels.list({...filters, grouped:true})`), and reworked the Admin Product Models table to 8 columns (added **Color** + **Storage**; `formatStorages()` helper sorts/dedupes GB→TB; media filter now a server-side `media` arg instead of the old client `filteredProducts` scan); rows navigate by `representative_id`. Product detail subtitle gained a muted note that photos/videos are shared across every storage+carrier variant of the color. Removed now-unused `useProductModels`/`ProductModelWithCounts`/`ProductRow` imports. Build clean; eslint clean on the 3 touched src files (repo-wide pre-existing errors untouched). 4 atomic commits 09abd24 / 0780e25 / 259f858 / 1ec5672. · touched: src/services/product-models.ts, src/hooks/use-product-models.ts, src/pages/admin/products.tsx, src/pages/admin/product-detail.tsx
- 2026-06-28 · Backorder identifier position + 3rd surface fix (v1.62.1): follow-up to v1.62.0 per Joey's screenshots. (1) The staff identifier now sits **right after the model name** (`Apple iPhone 16 Pro Max A3295 (MYWJ3J/A) / Desert Titanium / …`) instead of appended at the end — new shared helper `withBackorderIdentifier()` (src/lib/utils.ts) inserts it before the first ` / ` segment. (2) Added the identifier to the **Backorders page Lines tab** (`backorder-list.tsx` `getBackorderDescription`), which v1.62.0 missed. All three staff backorder surfaces (Admin Items, Backorders list, Messages Search Inventory) now use the one helper and read identically. Still kept out of `getItemDescription`/customer offers. Build + lint clean (0 new). · touched: src/lib/utils.ts, src/pages/admin/items.tsx, src/services/items.ts, src/components/backorders/backorder-list.tsx
- 2026-06-28 · Backorder follow-ups #2: richer B-row description + duplicate-link guard (v1.62.0): **(1) Description** — B-code rows showed `Apple iPhone 16 Pro Max / A18 Pro / 8 / 256 / 6.9"` (no color, no identifier) because the iosys-promoted product_models have `category_id=NULL` → `getItemDescription` takes the generic fallback that omitted color/model_number/part_number. Fix split by audience: **color** (a universal core spec customers want too) added to the fallback in BOTH copies (`src/lib/utils.ts` + `supabase/functions/_shared/item-description.ts`, ordered `model / color / cpu / ram / storage / screen`; golden tests updated, 8 deno tests green) so it shows on Admin Items, Messages search AND customer offers; the **staff-only identifier `A3295 (MYWJ3J/A)`** (model_number + part_number) appended AFTER the shared desc on the two staff backorder surfaces only — `getBackorderDesc` (items.tsx) + `searchAvailableBackorderLines` (services/items.ts) — kept OUT of `getItemDescription`/offer path. RPC `search_available_backorder_lines` now also returns `part_number` (migration `20260628150000`) so the two staff surfaces stay identical. Redeployed `generate-pending-drafts` + `test-ai-reply` (bundle `_shared/item-description`) so AI offers pick up color. **(2) Dedup** — same supplier listing was addable twice (confirmed B000003 == B000004, identical iosys /350998 URL). New app-level pre-submit guard `findExistingBackorderLine()` (services/backorders.ts) blocks on same supplier_url OR same SKU identity (product+grade+normalized storage+CI color) and names the existing B-code; DB backstop migration `20260628160000` collapses existing ACTIVE dupes to the oldest (CLOSED; only those with no order_items) then adds partial UNIQUE `(product_id, condition_grade, COALESCE(storage_gb,-1), lower(COALESCE(color,''))) WHERE status='ACTIVE'`. VERIFIED on live: only B000003 ACTIVE now (B000004 closed), RPC returns A3295/MYWJ3J/A, index built clean. Build + lint clean (0 new; pre-existing items.tsx SessionSale unused left per spec). · touched: src/lib/utils.ts, supabase/functions/_shared/item-description.ts(+test), src/pages/admin/items.tsx, src/services/{items,backorders}.ts, src/components/backorders/add-backorder-dialog.tsx, supabase/migrations/20260628150000_backorder_search_part_number.sql, supabase/migrations/20260628160000_backorder_dedup_guard.sql
- 2026-06-28 · Backorder follow-ups: Amount cell + customer pre-order ordering (v1.61.0): **(1) Admin Items "Amount" for B-rows** now shows the stacked **Cost / Sell / Disc / Profit** breakdown like P-rows (read-only `BackorderAmountCell`; Profit green = Sell − Disc − Cost). New **`backorder_lines.discount_amount`** column (migration `20260628140000`) — a pre-order can still be discounted — wired through database.types, Zod validator, the Add-Backorder dialog (new **Discount** field; pricing panel shows struck-through selling + "Customer Pays" effective + Profit), and **`search_available_backorder_lines`** now returns the discounted **effective price** (like sell-groups' `effective_price`), so AI offers / messaging Search-Inventory / Admin search all charge the discounted figure automatically (no shared-TS change → no offer edge-fn redeploy needed). **(2) `/mine/B…` customer ordering** (was dead — AI offers already send `/mine/B…` links): `parseCode`+`getClaimableBackorder` in mine.ts (type `'backorder'`, effective price, lead-time, curated `backorder_line_media`→product_media), claim.tsx renders a ⏳ **Pre-order** badge + working-day lead-time note, and **claim-mine** edge fn gets a B-branch that reserves via **`reserve_backorder_unit`** (locks line, re-checks availability, `quantity_reserved++`, inserts `order_item` AWAITING_ORDER) then backfills description/discount/qty. **Cancel/remove → RELEASE:** new **`release_backorder_unit(p_order_item_id)`** RPC (decrements `quantity_reserved` for open reservations only, nulls both `backorder_line_id`+`backorder_status` together to satisfy `order_items_backorder_chk`, idempotent) called from `cancelOrder` (per backorder line) + `removeOrderLineItem`. **E2E PASSED** against the live edge fn with B000003: order → available 1→0, order_item AWAITING_ORDER + unit_price ¥178,800 + desc set, order CONFIRMED ¥179,800; cancel → available 0→1, order_item unlinked, order CANCELLED; double-release no-op; discount ¥5,000 → search effective ¥173,800; test order cleaned up, B000003 pristine. Build + lint clean (0 new). claim-mine redeployed. · touched: supabase/migrations/20260628140000_backorder_discount.sql, supabase/functions/claim-mine/index.ts, src/services/{mine,orders}.ts, src/pages/mine/claim.tsx, src/pages/admin/items.tsx, src/components/backorders/add-backorder-dialog.tsx, src/validators/backorder.ts, src/lib/database.types.ts
- 2026-06-28 · Backorder (B-code) lines surface as sellable inventory (v1.60.0): pre-order backorder lines now appear in the two places staff look for stock, complementing the existing AI-offer path. **(1) Messaging "Search Inventory" dialog** — added `searchAvailableBackorderLines()` (items.ts, calls the existing `search_available_backorder_lines` RPC, status=ACTIVE+available>0) as a 4th parallel source in `useAvailableInventorySearch`; results show an amber "⏳ Pre-order" badge + lead-time and **insert with the pre-order block** (`⏳ Pre-order · N–N working days` + /mine/B-code link, mirroring offer-reply.ts `formatOfferBlock`). **(2) Admin Inventory (Items) page** — new **Backorder** chip (next to All/Products/Accessories/Group Codes) + ACTIVE pre-orders mixed into the **All** view as rows (only on the All/Available status tabs), found by the same description/code/category/brand/price filters; row shows B-code + grade + "⏳ Pre-order" + spec line + product hero, status col = "Pre-order (N avail)", supplier + sell price populated, row click → /admin/backorders. Added a `_kind:'backorder'` branch to `InventoryRow`, `unifiedColumns`, `sortInventoryRows`; `listBackorderLines` now also selects product_media for the thumbnail. **Monthly inventory report UNCHANGED — B-codes remain excluded** (generate_inventory_snapshot only counts items+accessories; verified). Build + lint clean (0 new issues); backorder search RPC verified returns B000003 for "iphone 16 pro max". · touched: src/services/items.ts, src/services/backorders.ts, src/hooks/use-items.ts, src/components/messaging/inventory-search-modal.tsx, src/pages/admin/items.tsx
- 2026-06-28 · Add-Backorder auto-select product model + identifier display/search (v1.59.0): after **Fetch**, the dialog now resolves the EXACT ACTIVE product_model and auto-selects it. New service `findMatchingProductModel()` (src/services/product-models.ts): **(a)** exact match on the listing's part-number (regex-extracted from modelText, e.g. `MYWJ3J/A`) == `product_models.part_number` (the SKU-precise happy path); **(b)** fallback = clean model name (strip A-number/storage, space-normalize `iPhone16`→`iPhone 16`) → `.ilike('model_name', cleanName)` (one model's ~30 SKUs, cap-safe) → pick by storage+color in JS. Returns the full row, **merged into the picker's product list** so an off-list match (beyond the >1000-row fetch cap) still displays + drives the spec auto-fill + mismatch banner. Picker search **decoupled** from the display label: full string stays on the "Parsed: …" line, a CLEAN term seeds `ProductPicker.initialSearch` so the manual fallback returns rows. **Identifier display + search:** backfilled `product_models.model_number` (A-number) from `iosys_catalog` (data-op `2026-06-28-backfill-product-model-numbers.sql`, 737 rows; A-number is model-level via part_number join) → ProductPicker now shows `A3295 (MYWJ3J/A)` per row and its (client-side) search matches `model_number` + `part_number` (paste "A3295" or "MYWJ3J/A" to find the row). Frontend + 1 service fn + 1 idempotent data-op; no edge/migration changes. Build + lint clean; match helpers unit-checked against real iosys modelText. **KNOWN LIMIT (deferred, ties to [[project_list_query_1000_cap]]):** the picker's identifier *search* is still client-side over the capped list — only auto-select (server-side) and the injected matched row reach beyond 1000 rows; a proper server-side picker search is the real fix. · touched: src/services/product-models.ts, src/components/backorders/add-backorder-dialog.tsx, src/components/intake/product-picker.tsx, supabase/data-ops/2026-06-28-backfill-product-model-numbers.sql
- 2026-06-28 · Backorder spec auto-fill from selected model + link-match guard (v1.58.0): when a Product Model is picked in Add-Backorder, the dialog fills the INTRINSIC specs the iosys listing doesn't carry (RAM, chip via `chipset`→CPU field, screen) from the model's absolute catalog values; storage + color stay as the LINK parsed them (source of truth — only empty fields are filled, never overwritten). Added a **mismatch warning** under the picker when the chosen model's storage/color contradicts the fetched listing (enforces "model must match the supplier link"). Explains why iPhone RAM/CPU were blank: iosys doesn't publish them + iPhone description_fields deliberately omit RAM/chip (chip lives in `chipset`, e.g. A18 Pro). Frontend-only (no DB/edge-fn). · touched: src/components/backorders/add-backorder-dialog.tsx
- 2026-06-28 · Backorder color i18n + price visualization (v1.57.0): (1) **bilingual color** — iosys parse now stores `color` = canonical ENGLISH (derived via `_shared/catalog/apple-colors.ts` `colorJaToEn`, falls back to raw JA if unmapped) for inventory + customer-facing, and `color_ja` = the original Japanese token for the Kaitori side. New `backorder_lines.color_ja` column (migration `20260628130000`); adapter `NormalizedSupplierProduct.colorJa`; dialog shows "Color (English)" + "Color (日本語)" fields. (2) **pricing summary panel** — Add-Backorder shows a live "Supplier + Markup = Selling" breakdown (big selling figure, amber note when selling is manually adjusted away from supplier+markup). Edge fn fetch-supplier-product redeployed (bundles the changed adapter + apple-colors). deno tests green (167). · touched: supabase/migrations/20260628130000_*, supabase/functions/_shared/supplier-adapters/{types,iosys}.ts(+test), src/{validators/backorder,lib/database.types}.ts, src/components/backorders/add-backorder-dialog.tsx
- 2026-06-28 · Backorder improvements (v1.56.0): (1) **reference-only suppliers** — `suppliers.is_reference_only` flag; "Iosys Tsushin Haiban-ka (Online)" set true → Add-Backorder auto-matches it as the iosys sourcing reference, and it's EXCLUDED from Intake supplier selection (`useSuppliers(_, {excludeReferenceOnly})`); (2) **per-line markup** — `backorder_lines.markup_jpy` (default ¥4000, editable up/down); dialog auto-computes Selling = Supplier + Markup via a watch effect, both stay editable; (3) **lead-time RANGE** — `lead_time_min_days` (default 4) added; **`lead_time_days` reused as the MAX/upper bound** (default 7); offers read "4–7 working days", list shows "4–7d". RPC `search_available_backorder_lines` + shared `mapBackorderRow`/`formatOfferBlock` updated (deno tests green, 167 pass). Migration `20260628120000` applied to remote. Edge fns generate-pending-drafts + test-ai-reply redeployed (bundle the changed _shared). · touched: supabase/migrations/20260628120000_*, supabase/functions/_shared/{offer-reply,inventory-search}.ts(+tests), src/{validators/backorder,services/suppliers,hooks/use-suppliers,lib/database.types}.ts, src/components/backorders/{add-backorder-dialog,backorder-list}.tsx, src/pages/admin/bulk-intake.tsx
- 2026-06-27 · Product-model accuracy Phase 4 (iPad + nostalgia iPhones): complete iosys iPad lineup for kaitori — 226 ACTIVE iPad SKUs/59 models (TABLET), new ipad-listing parser + verified ipad-specs (39 models), category-driven harvest, connectivity dimension; +43 old iPhones (5s/6/6 Plus/SE 1st gen) → iPhone 442 SKUs/45 models; fixed iPhone 12 Mini dupe · 38 deno tests green · migrations 20260627170000/180000 · data-ops phase4-ipad-reconcile/phase4b-ipad-synthesize/phase4-nostalgia-iphones/phase4-fix-12mini-dupes · touched: supabase/functions/_shared/catalog/*, harvest-iosys-catalog (redeployed), docs/PROJECT_STATE.md, spec
- 2026-06-27 · Product-model accuracy Phase 3 (fill gaps): +357 domestic iosys SKUs → ACTIVE product_models; iPhone catalog now 402 SKUs/42 models, searchable for new inventory · data-op supabase/data-ops/2026-06-27-phase3-fill-gaps.sql
- 2026-06-27 · Product-model accuracy Phase 2 (non-destructive reconcile, Decision C): iPhone product_models → SKU-precise (model,color,storage); 35 enriched-in-place (photos kept), 5 split, 7 stubs archived; 45 ACTIVE fully-enriched SKUs, 78/78 items storage-matched. Migration 20260627150000 (ARCHIVED status + superseded_by) · data-op supabase/data-ops/2026-06-27-phase2-iphone-reconcile.sql · touched: docs/PROJECT_STATE.md, spec §3b
- 2026-06-27 · Product-model accuracy Phase 1: harvested iosys → `iosys_catalog` (424 SKUs, part_number-keyed, carrier+lock+region resolved). Reusable edge fn `harvest-iosys-catalog` deployed (incremental upsert, junk-IP guarded) = the "new phones came in → re-run" path. Parser+crawl core in `_shared/catalog/` (iosys-listing.ts, harvest.ts), 20 deno tests green · migration 20260627140000 (unique part_number) · touched: supabase/functions/_shared/catalog/*, supabase/functions/harvest-iosys-catalog/index.ts
- 2026-06-27 · Image-search "Search web for more": built free Bing scraper provider (parseBingImageResults TDD-correct, fixture-proven) but live from Supabase edge IP it returns query-independent junk (datacenter-IP fallback) → reverted default to key-gated google_cse (button disabled until key/proxy); bing kept behind IMAGE_SEARCH_PROVIDER=bing_scrape · touched: supabase/functions/_shared/image-search/{bing.ts,bing.test.ts,provider.ts,__fixtures__}
- 2026-06-27 · Backorder Task 17: docs (SYSTEM_MAP "Backorder / Pre-order" section + PROJECT_STATE) + DEPLOY — redeployed generate-pending-drafts + test-ai-reply (bundle changed _shared inventory-search/offer-reply so backorder pre-order offers go live); backorder edge fns (fetch-supplier-product, search-product-images, save-backorder-photos) already deployed; all migrations on remote · v1.53.0 pushed to main (Vercel deploy triggered) · PROD SMOKE GREEN: search_available_backorder_lines RPC → 200, fetch-supplier-product unknown-host → 422 · REMAINING (human acceptance): add a real iosys URL through the live UI once Vercel finishes + run one full add→reserve→mark-ordered→swap with a test order · touched: docs/SYSTEM_MAP.md, docs/PROJECT_STATE.md, package.json
- 2026-06-27 · Backorder Task 16: AI offer pre-order badge + lead-time line (⏳ Pre-order · ~Nd) on backorder offers, P/G offers unchanged; formatOfferBlock branches on type==='backorder'; backorder flows generate-draft→searchInventory→offerCatalog→assembleOfferReply (TDD 16 tests) · touched: supabase/functions/_shared/offer-reply.ts(+test) · NOTE: messaging edge fns must be REDEPLOYED (bundle _shared) in Task 17
- 2026-06-27 · Backorder Task 15: swap dialog (QRScannerCamera + manual P-code → getItemForSwap → live core-spec ✓/✗ via frontend verifyPCodeMatch in utils.ts → fulfillBackorderWithItem; Confirm gated on match.ok+AVAILABLE; server-authoritative errors; invalidates to-fulfill/lines/items) · build+lint clean · touched: src/components/backorders/swap-dialog.tsx, src/pages/admin/backorders.tsx, src/lib/utils.ts, src/services/items.ts
- 2026-06-27 · Backorder Task 14: To-Fulfill worklist (groups AWAITING_ORDER/ORDERED/READY + procurement summary aggregating N× per B-line w/ supplier link + Mark-ordered mutation; onSwap seam for Task 15) + Lines/To-Fulfill tab on page · build+lint clean · touched: src/components/backorders/to-fulfill.tsx(+index), src/pages/admin/backorders.tsx
- 2026-06-27 · Backorder Task 13: add-backorder paste-to-add dialog (fetchSupplierProduct prefill → ProductPicker+supplier select → photo curate grid dedupe-prefer-_L → createBackorderLine + saveBackorderPhotos; "Search web" disabled w/ tooltip until IMAGE_SEARCH key set; partial-failure tolerant) + Zod validator · build+lint clean · touched: src/components/backorders/add-backorder-dialog.tsx, src/validators/backorder.ts, index.ts, backorders.tsx
- 2026-06-27 · Backorder Task 12: backorder list view (B-code/spec via getItemDescription/grade/supplier/prices/avail-res-recv/lead/status; status tabs; reuses GradeBadge/StatusBadge/skeleton/empty) · build passes · touched: src/pages/admin/backorders.tsx, src/components/backorders/backorder-list.tsx(+index)
- 2026-06-27 · Backorder Task 11: /admin/backorders lazy route + Inventory sidebar entry (after New Intake, PackagePlus icon) + stub page · build passes · touched: src/components/layout/sidebar.tsx, src/pages/admin/backorders.tsx, router
- 2026-06-27 · Backorder Task 10: backorders service (15 fns) + reserve_backorder_unit RPC + regen types · ALSO fixed pre-existing types.ts corruption (banner text from a05aab3's `gen types > types.ts`; restored alias layer + added backorder aliases) + normalizeStorageGb in utils.ts & _shared verifier (6 deno tests) · build passes · touched: src/services/backorders.ts, src/lib/{types,database.types,utils}.ts, supabase/functions/_shared/backorder-match.ts(+test), supabase/migrations/20260627120400_reserve_backorder.sql
- 2026-06-27 · Backorder Task 9: transactional swap + mark-ordered RPCs (fulfill_backorder_with_item: eligibility + core-spec hard-block → item RESERVED + line.quantity_received++; mark_backorder_ordered) · applied + live seed test (hard-block fires, happy path works w/ text '128GB' vs int 128 via IMMUTABLE _backorder_norm_storage_gb helper) · touched: supabase/migrations/20260627120300_backorder_fulfillment_rpcs.sql
- 2026-06-27 · Backorder Task 8: core-spec match verifier verifyPCodeMatch (pure, TDD 3 tests; CORE_FIELDS product_id/storage_gb/color/condition_grade aligned w/ Task 9 RPC; color case/space-insensitive) · touched: supabase/functions/_shared/backorder-match.ts(+test)
- 2026-06-27 · Backorder Task 7c: save-backorder-photos edge fn (server-side copy of kept iosys/web photos → backorder-media bucket + backorder_line_media rows; skips 404s, never fails batch) · DEPLOYED · smoke: real upload+public URL serves bytes, skip verified, temp data cleaned · touched: supabase/functions/save-backorder-photos/index.ts
- 2026-06-27 · Backorder Task 7b: optional pluggable web image-search provider (ImageSearchProvider interface + Google CSE impl; degrades to {configured:false,images:[]} with no env key) · DEPLOYED · smoke: no-key→configured:false, missing-query→400 · touched: supabase/functions/_shared/image-search/{types,provider}.ts, supabase/functions/search-product-images/index.ts
- 2026-06-27 · Backorder Task 7: fetch-supplier-product edge fn (resolveAdapter→fetch→parse→normalized product JSON; browser UA; 400/422/502/500 handling) · DEPLOYED · live smoke: iosys 384323 returns full product (Apple iPhone15 Plus, ¥104800, stock 272, grade S); unknown host → 422 · touched: supabase/functions/fetch-supplier-product/index.ts
- 2026-06-27 · Backorder Task 6: supplier adapter registry (resolveAdapter by host, TDD — 2 tests pass) · touched: supabase/functions/_shared/supplier-adapters/registry.ts, registry.test.ts
- 2026-06-27 · Backorder Task 5: iosys adapter → NormalizedSupplierProduct (JSON-LD + DOM parse, rank→grade, gallery; TDD 4 tests; real fixture iosys-384323.html) · touched: supabase/functions/_shared/supplier-adapters/{types,iosys}.ts(+test,fixture)
- 2026-06-27 · Backorder Task 4: backorder result type in shared inventory-search (mapBackorderRow mirrors sell-group mapping; 3rd RPC in parallel; B-code exact-match branch; 6 tests) · touched: supabase/functions/_shared/inventory-search.ts(+test)
- 2026-06-27 · Backorder Task 3: search_available_backorder_lines RPC (mirrors sell-groups return shape for drop-in mapper) · touched: supabase/migrations/20260627120200_search_backorder_lines.sql
- 2026-06-27 · Backorder Task 2: order_items backorder linkage (backorder_line_id, backorder_status enum, item_id nullable, CHECK permitting existing ad-hoc lines) · applied to remote (1770 rows, 0 violations) · touched: supabase/migrations/20260627120100_order_items_backorder.sql
- 2026-06-27 · Backorder Task 1: backorder_lines (+ generated available col) + backorder_line_media tables, b_code_seq, backorder-media storage bucket · applied to remote, generate_code('B','b_code_seq')→B000001 verified · NOTE: spec's photo_group_id FK omitted (live schema dropped photo_groups; photos now via product_media / backorder_line_media) · touched: supabase/migrations/20260627120000_backorder_lines.sql
- 2026-06-27 · Fix: strip Markdown from outbound AI replies (Messenger renders plaintext) — normalizeOutboundText at generate-draft + send-via-missive · touched: supabase/functions/_shared/normalize-markdown.ts(+test), generate-draft.ts, send-via-missive.ts · DEPLOYED (send-message, generate-pending-drafts, test-ai-reply)
- 2026-06-27 · Project command center: state hub + system map + enforcement hooks · touched: docs/, .claude/, CLAUDE.md
- 2026-06-26 · Sub-intent autonomy engine (backend): classify→autonomy pipeline OFF/DRAFT/SEND · v1.52.0 · touched: supabase/functions (generate-draft, send-message, _shared), messaging tables
- 2026-06-26 · Fix AI draft cron: ambiguous ai_enabled + missing edge-fn credentials · touched: messaging cron / edge fns
- 2026-06-19 · Rich sell-group AI offers (spec line like items) · v1.51.0 · touched: messaging.ts, sell-groups.ts, offers RPC
- 2026-06-18 · AI emoji offer format ({{OFFER:CODE}} token, code-assembled block) · v1.50.0 · touched: messaging AI
- 2026-06-17 · Sales Specialist v2 (qualify-then-handoff + in-process search_inventory) · v1.48.0
- 2026-06-17 · Messages AI redesign Plans 1-3 + 3b (specialist playbooks) · v1.47.0

## Deferred / known issues
- List-query 1000-row cap: admin lists fetch whole tables, hit PostgREST 1000-row cap. Mitigated v1.46.1. · ref: docs/investigations/list-query-1000-row-cap.md
- AI provider config split: two provider tables/UIs (ai_configurations vs ai_providers); consolidation idea pending.

## Links
- [System map](./SYSTEM_MAP.md) · [Specs](./superpowers/specs/) · [Plans](./superpowers/plans/) · [Investigations](./investigations/) · [Runbooks](./runbooks/)
